#include<iostream>
using namespace std;
// It also stores only ne single value at a time and only the string ones.
enum TrafficLight{
    RED,// All letters should be capital
    GREEN,
    YELLOW
};
int main(){
    TrafficLight tfl = YELLOW;
    cout << tfl << endl;
    // It gives the index at which place that element is present.
}